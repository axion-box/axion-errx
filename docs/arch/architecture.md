# 仓库架构

当前仓库采用轻量结构：

- `pkgs/errx/`: 对外共享的统一错误模型与通用错误码
- `scripts/agents/`: 统一门禁入口与最小必要检查
- `docs/`: agent 规则、需求基线与架构说明

设计原则：

- 只放共享错误能力，不放应用层运行时
- 共享包目录清晰稳定，便于被 `axion-agent` 和 `axion-bridge` 直接引用
- 脚本和文档规模与仓库复杂度匹配
