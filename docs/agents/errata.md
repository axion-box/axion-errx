# AI 易错勘误

- 本仓库是共享错误基础库，不要把 `axion-agent` / `axion-bridge` 的应用层依赖直接搬进来。
- 通用错误码段 `1000~1999` 归共享层维护；业务私有错误码应留在各自仓库定义。
- `pkgs/errx` 是唯一对外共享入口；如需内部辅助包，后续再单独论证。
- `Error` 的职责是表达统一错误模型，不要顺手塞进 HTTP 状态码、日志实现或传输层协议常量。
